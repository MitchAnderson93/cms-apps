name: Build development dist release

on:
  push:
    branches: [development]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  dist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install + build
        run: |
          corepack enable
          corepack prepare pnpm@9.15.0 --activate
          pnpm install
          pnpm build:app --config example.json --app react

      - name: Stage build output + stable aliases
        run: |
          rm -rf /tmp/dist
          mkdir -p /tmp/dist
          cp -R apps/react/dist/. /tmp/dist/

          cp "$(ls -1 /tmp/dist/assets/*.js 2>/dev/null | head -n 1)" /tmp/dist/dist.js
          if ls -1 /tmp/dist/assets/*.css >/dev/null 2>&1; then
            cp "$(ls -1 /tmp/dist/assets/*.css | head -n 1)" /tmp/dist/dist.css
          fi

      - name: Publish to dist branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Use config name in branch (e.g., dist-example, dist-production)
          DIST_BRANCH="dist-react"

          git checkout --orphan $DIST_BRANCH
          git rm -rf . || true
          git clean -fdx

          cp -R /tmp/dist/. .

          git add -A
          git commit -m "dist: ${GITHUB_SHA}"
          git push -f origin $DIST_BRANCH